---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-nfs-share
spec:
  storageClassName: ${storageClassName}
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: ${pvc.dataSize}
